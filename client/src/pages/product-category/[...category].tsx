import { useRouter } from "next/router";
import React from "react";

const categoryProducts = () => {
  const router = useRouter();

  return (
    <div className="sm:max-w-[1200px] mx-auto my-10">categoryProducts</div>
  );
};

export default categoryProducts;
